name: tm3l-protocol-time-machine

services:
  postgres:
    image: postgres:17-alpine
    container_name: protocol-db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-protocol}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-protocol_secret}
      POSTGRES_DB: ${POSTGRES_DB:-protocol_dev}
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-protocol} -d ${POSTGRES_DB:-protocol_dev}"]
      interval: 5s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 512M

  pocketbase:
    image: ghcr.io/muchobien/pocketbase:latest
    container_name: protocol-pb
    ports:
      - "8094:8090"
    volumes:
      - pb_data:/pb_data
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8090/api/health"]
      interval: 5s
      timeout: 3s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M

  server:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    container_name: protocol-server
    environment:
      PORT: "8080"
      DATABASE_URL: "postgres://${POSTGRES_USER:-protocol}:${POSTGRES_PASSWORD:-protocol_secret}@postgres:5432/${POSTGRES_DB:-protocol_dev}?sslmode=disable"
      POCKETBASE_URL: "http://pocketbase:8090"
      ENV: "development"
    ports:
      - "8084:8080"
    depends_on:
      postgres:
        condition: service_healthy
      pocketbase:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 512M

  explorer:
    build:
      context: ./explorer
    container_name: protocol-explorer
    environment:
      VITE_API_URL: "http://localhost:8084"
      VITE_PB_URL: "http://localhost:8094"
    ports:
      - "5176:5173"
    volumes:
      - ./explorer:/app
      - /app/node_modules
    deploy:
      resources:
        limits:
          memory: 384M

volumes:
  postgres_data:
  pb_data:
